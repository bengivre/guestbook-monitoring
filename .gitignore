# Dependencies
node_modules/

# TypeScript build output (tsconfig outDir)
bin/

# Logs
*.log

# Local env / secrets (scripts/deploy.sh loads .env or --env-file prod.env)
.env
*.env
!**/.env.example

# OS
.DS_Store

# Pulumi workspace metadata (state lives in the backend, not here)
.pulumi/
